"use client";

export function ProBadge() {
  return (
    <span className="inline-flex items-center px-1.5 py-0.5 rounded-full bg-accent-dim text-accent text-[9px] font-bold uppercase tracking-wider font-[family-name:var(--font-outfit)]">
      PRO
    </span>
  );
}
